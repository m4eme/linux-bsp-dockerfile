version: '2.3'
services:
    petalinux:
      tty: true
      container_name: 'petalinux_2021_2'
      volumes:
          - '/opt:/opt'
          - '/home/${USER}:/home/mathworks'
          - '/local:/local'
      image: adi_petalinux:2021.2
      build: 
        context: .
        args:
          PUID: ${DOCKER_UID} 
          PGID: ${GID:-101}
